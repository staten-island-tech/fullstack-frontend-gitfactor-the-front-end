import * as userData from "./modules/userData";
import * as gameItems from "./modules/gameItems";
import { createStore } from "vuex";

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: { userData, gameItems },
});
